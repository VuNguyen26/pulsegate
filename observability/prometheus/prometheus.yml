global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "pulsegate-api-gateway"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "api-gateway:3000"
        labels:
          service: "api-gateway"
